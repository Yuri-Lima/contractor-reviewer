{
  "name": "api",
  "tags": ["scope:api"],
  "targets": {
    "worker": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start --entryFile worker",
        "cwd": "apps/api"
      }
    },
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build --platform ${BUILD_PLATFORM:-linux/amd64} -f apps/api/Dockerfile -t apps-api:${IMAGE_TAG:-latest} .",
        "cwd": "{workspaceRoot}",
        "env": {}
      },
      "dependsOn": ["build", "^build"],
      "configurations": {}
    }
  }
}
