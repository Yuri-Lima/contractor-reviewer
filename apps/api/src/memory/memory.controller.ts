import { Controller } from '@nestjs/common';

/**
 * MemoryController - placeholder for future memory-related endpoints.
 * Memory is managed internally via MemoryService (SummarizeMemory job, RAG injection, DSAR export).
 */
@Controller()
export class MemoryController {}
