{
  "name": "web",
  "tags": ["scope:web"],
  "targets": {
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build --platform ${BUILD_PLATFORM:-linux/amd64} -f apps/web/Dockerfile -t apps-web:${IMAGE_TAG:-latest} .",
        "cwd": "{workspaceRoot}",
        "env": {}
      },
      "dependsOn": ["build", "^build"],
      "configurations": {}
    }
  }
}
