{
  "name": "web",
  "tags": ["scope:web"],
  "targets": {
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/web/Dockerfile -t apps-web:${IMAGE_TAG:-latest} .",
        "cwd": "{workspaceRoot}",
        "env": {}
      },
      "dependsOn": ["build", "^build"],
      "configurations": {}
    }
  }
}
