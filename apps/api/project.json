{
  "name": "api",
  "tags": ["scope:api"],
  "targets": {
    "worker": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start --entryFile worker",
        "cwd": "apps/api"
      }
    },
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -f apps/api/Dockerfile --tag apps-api .",
        "cwd": "{workspaceRoot}",
        "env": {}
      },
      "dependsOn": ["build", "^build"],
      "configurations": {}
    }
  }
}
