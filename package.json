{
  "name": "contractai-review",
  "version": "0.1.0",
  "private": true,
  "description": "ContractAI Review MVP+ — monorepo",
  "scripts": {
    "dev:api": "nx run api:dev",
    "start:api": "nx run api:start",
    "start:worker": "nx run api:start:worker",
    "dev:web": "nx run web:serve",
    "dev:all": "nx run-many --targets=dev,worker -p api,web --parallel=3",
    "build": "nx run-many -t build -p shared,api,web",
    "build:api": "nx run api:build",
    "build:web": "nx run web:build",
    "migration:run": "nx run api:migration:run",
    "migration:generate": "nx run api:migration:generate",
    "e2e": "nx run web:e2e",
    "kill:port": "./scripts/kill-port.sh 3000 4400 8000 8001",
    "docker:build:push": "./scripts/build-and-push.sh",
    "verify:deploy": "./scripts/verify-deployment.sh",
    "verify:parsers": "./scripts/verify-parsers.sh",
    "deploy:up": "cd deploy && docker compose pull && docker compose up -d",
    "deploy:down": "cd deploy && docker compose down",
    "deploy:restart": "cd deploy && docker compose restart api web",
    "deploy:logs": "cd deploy && docker compose logs -f",
    "deploy:ps": "cd deploy && docker compose ps",
    "lint": "nx run-many -t lint --all",
    "test": "nx run-many -t test --all",
    "docker:build": "./scripts/docker-build.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@nx/angular": "22.5.2",
    "@nx/docker": "22.5.2",
    "@nx/eslint": "^22.5.2",
    "@nx/eslint-plugin": "^22.5.2",
    "@nx/playwright": "22.5.2",
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "eslint": "^9.39.3",
    "globals": "^17.3.0",
    "jest": "^30.2.0",
    "nx": "22.5.2",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}